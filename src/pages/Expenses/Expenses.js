import React, { Component } from 'react';
import styles from './Expenses.module.scss';

class Expenses extends Component {
  render() {
    return <h1 className={styles.title}>Expenses</h1>;
  }
}

export default Expenses;
